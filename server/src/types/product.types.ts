export interface Products {
    id: number,
    title: string,
    price: number,
    created_from: string,
    is_stock: boolean,
    weight: number,
    compount: string[],
    created_date: string
}