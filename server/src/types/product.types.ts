export interface Products {
    id: number,
    title: string,
    price: number,
    created_from: string,
    is_stock: boolean,
    weight: number,
    created_date: string,
    property: string[]
}